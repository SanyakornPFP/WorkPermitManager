@{
    ViewData["Controller"] = "งานบริการ";
    ViewData["Action"] = "ประเภทรายการที่รับทำ";
    ViewData["Icon"] = "cog";
}
<!-- Container-fluid starts-->
<div class="container-fluid">
    <div class="row" style="display: none" id="showInsert">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <a class="btn btn-light" id="createOff" href="#" style="float:right"><i class="fa fa-close"></i> ยกเลิก</a>
                    <h4 id="createHeader"><i class="fa fa-plus text-dark"></i> เพิ่มข้อมูล</h4>
                    <h4 id="updateHeader" style="display:none"><i class="fa fa-edit"></i> แก้ไขข้อมูล</h4>
                </div>
                <div class="card-body">
                    <form id="myForm" class="row g-3" method="post">
                        <input class="form-control" name="ServiceItemID" id="txtServiceItemID" type="hidden">
                        <div class="col-xxl-6 col-sm-6">
                            <label class="col-form-label" for="txtServiceItemName">ชื่อรายการบริการ</label>
                            <input class="form-control" name="ServiceItemName" id="txtServiceItemName" type="text" placeholder="กรุณากรอกข้อมูล">
                        </div>
                        <div class="col-xxl-6 col-sm-6">
                            <label class="col-form-label" for="txtDescriptions">คำอธิบาย</label>
                            <input class="form-control" name="Descriptions" id="txtDescriptions" type="text" placeholder="กรุณากรอกข้อมูล">
                        </div>
                        <div class="col-xxl-12 col-sm-12">
                            <label class="col-form-label" for="txtNote">หมายเหตุ</label>
                            <textarea class="form-control" name="Note" id="txtNote" type="text" rows="5" placeholder="กรุณากรอกข้อมูล"></textarea>
                        </div>
                        <div class="col-sm-12 col-xxl-12 col-12 justify-content-between d-flex">
                            <button class="btn btn-primary" id="createBtn" type="submit" title="btn btn-pill btn-success active">บันทึก</button>
                            <button class="btn btn-primary" id="updateBtn" type="submit" title="btn btn-pill btn-info active" style="display: none;">อัพเดท</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-sm-12">
            <div class="card">
                <div class="card-header">
                    <div class="ribbon ribbon-primary ribbon-clip">
                        <i class="fa fa-cog"></i> ประเภทรายการที่รับทำ
                    </div>
                    <div class="d-flex gap-2 justify-end" style="float:right">
                        <a class="btn btn-primary btn-sm" id="createOn" href="#">
                            <i class="fa fa-plus text-white"></i> เพิ่มรายการบริการ
                        </a>
                        <a class="btn btn-light btn-sm" asp-action="Services" asp-controller="Services">
                            <i class="fa fa-caret-left text-dark"></i> ย้อนกลับ
                        </a>
                    </div>
                </div>
                <div class="card-body">
                    <div class="table-responsive theme-scrollbar mt-2">
                        <table class="display table table-bordered" id="data-source-1" style="width:100%">
                            <thead>
                                <tr>
                                    <th>ชื่อประเภทรายการที่รับทำ</th>
                                    <th>คำอธิบาย</th>
                                    <th>หมายเหตุ</th>
                                    <th>ดำเนินการ</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in ViewBag.ServiceList)
                                {
                                    <tr>
                                        <td>@item.ServiceItemName</td>
                                        <td>@item.Descriptions</td>
                                        <td style="white-space: pre-line;">@item.Note</td>
                                        <td>
                                            <ul class="action">
                                                <li class="edit">
                                                    <a href="#" class="editBtn" data-ServiceItemID="@item.ServiceItemID">
                                                        <i class="icon-pencil-alt"></i>
                                                    </a>
                                                </li>
                                                <li class="delete">
                                                    <a href="#" class="deleteBtn" data-ServiceItemID="@item.ServiceItemID">
                                                        <i class="icon-trash"></i>
                                                    </a>
                                                </li>
                                            </ul>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="serviceItemDetailsModal" tabindex="-1" aria-labelledby="serviceItemDetailsModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="serviceItemDetailsModalLabel"><i class="fa fa-list-alt"></i> รายละเอียดรายการบริการ</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <table class="table table-bordered">
                    <tbody>
                        <tr>
                            <th class="bg-light" scope="row">ชื่อรายการบริการ</th>
                            <td><span id="modalServiceItemName"></span></td>
                        </tr>
                        <tr>
                            <th class="bg-light">คำอธิบาย</th>
                            <td><span id="modalDescriptions"></span></td>
                        </tr>
                        <tr>
                            <th class="bg-light">หมายเหตุ</th>
                            <td><span id="modalNote"></span></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-light" data-bs-dismiss="modal">ปิด</button>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var showInsert = document.getElementById('showInsert');
            var createOn = document.getElementById('createOn');
            var createOff = document.getElementById('createOff');

            // Event listener for "เพิ่ม" button
            createOn.addEventListener('click', function (e) {
                e.preventDefault();
                showInsert.style.display = 'block';  // Show the form
            });

            // Event listener for "ยกเลิกเพิ่ม" button
            createOff.addEventListener('click', function (e) {
                e.preventDefault();
                showInsert.style.display = 'none';  // Hide the form
                // Reset the form fields
                $('#myForm')[0].reset();
                // Hide the ServiceItemID field and update button
                $('#txtServiceItemID').hide();
                $('#updateBtn').hide();
                // Show the create button and header
                $('#createHeader').show();
                $('#updateHeader').hide();
                $('#createBtn').show();
                // Hide the cancel button
                $('#cancelBtn').hide();
            });

            document.querySelectorAll('.detailBtn').forEach(function (button) {
                button.addEventListener('click', function (e) {
                    e.preventDefault();
                    var serviceItemID = this.getAttribute('data-ServiceItemID');

                    $.ajax({
                        url: '@Url.Action("GetServiceItemDetails", "Services")',
                        type: 'POST',
                        data: { ServiceItemID: serviceItemID },
                        success: function (response) {
                            if (response.success == false) {
                                Swal.fire('ผิดพลาด', response.message, 'error');
                            } else {
                                var data = response.data;
                                $('#modalServiceItemName').text(data.serviceItemName);
                                $('#modalDescriptions').text(data.descriptions);
                                $('#modalNote').text(data.note);

                                $('#serviceItemDetailsModal').modal('show');
                            }
                        },
                        error: function (xhr, textStatus, errorThrown) {
                            console.error('Error:', xhr.status, textStatus, errorThrown);
                            handleAjaxError(xhr);
                        }
                    });
                });
            });

            document.getElementById('createBtn').addEventListener('click', function (e) {
                e.preventDefault();

                var formData = new FormData($('#myForm')[0]);
                var serviceItemName = $('#txtServiceItemName').val();
                var serviceItemID = $('#txtServiceItemID').val();

                // ตรวจสอบชื่อรายการบริการก่อน
                $.ajax({
                    url: '@Url.Action("CheckServiceItemName", "Services")',
                    type: 'POST',
                    data: { ServiceItemName: serviceItemName, ServiceItemID: serviceItemID },
                    success: function (response) {
                        if (response.success == false) {
                            Swal.fire('ผิดพลาด', response.message, 'error');
                        } else {
                            // ถ้าชื่อรายการบริการไม่ซ้ำ ให้ทำการบันทึกข้อมูล
                            Swal.fire({
                                title: 'คุณแน่ใจหรือไม่?',
                                text: "คุณจะไม่สามารถย้อนกลับได้!",
                                icon: 'warning',
                                showCancelButton: true,
                                confirmButtonColor: '#28a745', // Light green
                                cancelButtonColor: '#d3d3d3', // Light gray
                                confirmButtonText: 'ใช่, บันทึก!',
                                cancelButtonText: 'ยกเลิก'
                            }).then((result) => {
                                if (result.isConfirmed) {
                                    $.ajax({
                                        url: '@Url.Action("CreateServiceItem", "Services")',
                                        type: 'POST',
                                        data: formData,
                                        processData: false,
                                        contentType: false,
                                        success: function (response) {
                                            if (response.success == true) {
                                                Swal.fire(
                                                    'บันทึกแล้ว!',
                                                    'ข้อมูลของคุณถูกบันทึกแล้ว.',
                                                    'success'
                                                ).then((result) => {
                                                    if (result.isConfirmed) {
                                                        window.location.reload();
                                                    }
                                                });
                                            } else {
                                                Swal.fire('ผิดพลาด', response.message, 'error');
                                            }
                                        },
                                        error: function (xhr, textStatus, errorThrown) {
                                            console.error('Error:', xhr.status, textStatus, errorThrown);
                                            handleAjaxError(xhr);
                                        }
                                    });
                                }
                            });
                        }
                    },
                    error: function (xhr, textStatus, errorThrown) {
                        console.error('Error:', xhr.status, textStatus, errorThrown);
                        handleAjaxError(xhr);
                    }
                });
            });

            // แก้ไขข้อมูลรายการบริการ
            document.querySelectorAll('.editBtn').forEach(function (button) {
                button.addEventListener('click', function (e) {
                    e.preventDefault();
                    var serviceItemID = this.getAttribute('data-ServiceItemID');

                    $.ajax({
                        url: '@Url.Action("GetServiceItemDetails", "Services")',
                        type: 'POST',
                        data: { ServiceItemID: serviceItemID },
                        success: function (response) {
                            if (response.success == false) {
                                Swal.fire('ผิดพลาด', response.message, 'error');
                            } else {
                                $('#showInsert').show();
                                $('#txtServiceItemID').val(response.data.serviceItemID);
                                $('#txtServiceItemName').val(response.data.serviceItemName);
                                $('#txtDescriptions').val(response.data.descriptions);
                                $('#txtNote').val(response.data.note);

                                $('#createHeader').hide();
                                $('#updateHeader').show();
                                $('#createBtn').hide();
                                $('#updateBtn').show();
                            }
                        },
                        error: function (xhr, textStatus, errorThrown) {
                            console.error('Error:', xhr.status, textStatus, errorThrown);
                            handleAjaxError(xhr);
                        }
                    });
                });
            });

            // ลบข้อมูลรายการบริการ
            document.querySelectorAll('.deleteBtn').forEach(function (button) {
                button.addEventListener('click', function (e) {
                    e.preventDefault();
                    var serviceItemID = this.getAttribute('data-ServiceItemID');

                    Swal.fire({
                        title: 'คุณแน่ใจหรือไม่?',
                        text: "คุณจะไม่สามารถย้อนกลับได้!",
                        icon: 'warning',
                        showCancelButton: true,
                        confirmButtonColor: '#d33',
                        cancelButtonColor: '#d3d3d3',
                        confirmButtonText: 'ใช่, ลบ!',
                        cancelButtonText: 'ยกเลิก'
                    }).then((result) => {
                        if (result.isConfirmed) {
                            $.ajax({
                                url: '@Url.Action("DeleteServiceItem", "Services")',
                                type: 'POST',
                                data: { ServiceItemID: serviceItemID },
                                success: function (response) {
                                    if (response.success) {
                                        Swal.fire(
                                            'ลบแล้ว!',
                                            response.message,
                                            'success'
                                        ).then((result) => {
                                            if (result.isConfirmed) {
                                                window.location.reload();
                                            }
                                        });
                                    } else {
                                        Swal.fire('ผิดพลาด', response.message, 'error');
                                    }
                                },
                                error: function (xhr, textStatus, errorThrown) {
                                    console.error('Error:', xhr.status, textStatus, errorThrown);
                                    handleAjaxError(xhr);
                                }
                            });
                        }
                    });
                });
            });

            document.getElementById('updateBtn').addEventListener('click', function (e) {
                e.preventDefault();

                var formData = new FormData($('#myForm')[0]);
                var serviceItemName = $('#txtServiceItemName').val();
                var serviceItemID = $('#txtServiceItemID').val();

                // ตรวจสอบชื่อรายการบริการก่อน
                $.ajax({
                    url: '@Url.Action("CheckServiceItemName", "Services")',
                    type: 'POST',
                    data: { ServiceItemName: serviceItemName, ServiceItemID: serviceItemID },
                    success: function (response) {
                        if (response.success == false) {
                            Swal.fire('ผิดพลาด', response.message, 'error');
                        } else {
                            // ถ้าชื่อรายการบริการไม่ซ้ำ ให้ทำการอัปเดตข้อมูล
                            Swal.fire({
                                title: 'คุณแน่ใจหรือไม่?',
                                text: "คุณจะไม่สามารถย้อนกลับได้!",
                                icon: 'warning',
                                showCancelButton: true,
                                confirmButtonColor: '#007bff', // Light blue
                                cancelButtonColor: '#d3d3d3', // Light gray
                                confirmButtonText: 'ใช่, บันทึก!',
                                cancelButtonText: 'ยกเลิก'
                            }).then((result) => {
                                if (result.isConfirmed) {
                                    $.ajax({
                                        url: '@Url.Action("UpdateServiceItem", "Services")',
                                        type: 'POST',
                                        data: formData,
                                        processData: false,
                                        contentType: false,
                                        success: function (response) {
                                            if (response.success == true) {
                                                Swal.fire(
                                                    'บันทึกแล้ว!',
                                                    'ข้อมูลของคุณถูกบันทึกแล้ว.',
                                                    'success'
                                                ).then((result) => {
                                                    if (result.isConfirmed) {
                                                        window.location.reload();
                                                    }
                                                })
                                            } else {
                                                Swal.fire('ผิดพลาด', response.message, 'error');
                                            }
                                        },
                                        error: function (xhr, textStatus, errorThrown) {
                                            handleAjaxError(xhr);
                                        }
                                    });
                                }
                            });
                        }
                    },
                    error: function (xhr, textStatus, errorThrown) {
                        console.error('Error:', xhr.status, textStatus, errorThrown);
                        handleAjaxError(xhr);
                    }
                });
            });

            function handleAjaxError(xhr) {
                console.log('Error:', xhr.status, xhr.statusText);

                switch (xhr.status) {
                    case 400:
                        window.open('@Url.Action("Error400", "Error")', '_blank');
                        break;
                    case 401:
                        window.open('@Url.Action("Error401", "Error")', '_blank');
                        break;
                    case 403:
                        window.open('@Url.Action("Error403", "Error")', '_blank');
                        break;
                    case 404:
                        window.open('@Url.Action("Error404", "Error")', '_blank');
                        break;
                    case 500:
                        window.open('@Url.Action("Error500", "Error")', '_blank');
                        break;
                    case 503:
                        window.open('@Url.Action("Error503", "Error")', '_blank');
                        break;
                    default:
                        console.log('An unexpected error occurred: ', xhr.status);
                }
            }
        });
    </script>
}